<%= stylesheet_link_tag "visual-guide-editorial" %>
<%= javascript_include_tag 'visual-guide-editorial' %>

<%= render "site/header_page", :title => 'Guía visual Editorial' %>

<div class="books-box play-video">
  <div class="content">
    <h4>Editorial</h4>
    <h3>Estás consultando los vídeos del libro "<%= @book.title %>"</h3>
    <p>Si quieres ver otro, introduce su <b>código</b></p>
    <%= link_to '',visual_guide_editorial_path, :class => 'back' %>
    <%= form_tag visual_guide_editorial_video_path, :method => :post, :remote => true do %>
      <%= text_field_tag :code, @book_video.code, :placeholder => "introduce el código" %>
      <%= hidden_field_tag "id", @book.id %>
      <%= submit_tag "ver vídeo" %>
    <% end %>

    <div id="video">
      <%=raw @book_video.video %>
    </div>

  </div>
  <span class="shadow"></span>
</div>

